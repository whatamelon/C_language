180608 c언어 데이터 정렬
선택 정렬 과정

 - 가장 작은 원소를 가장 앞에 있는 원소와 바꿔준다.
 - 원소 검색 수 줄이기
 - 가장 작은 원소를 n으로 설정함
 - min 값 찾고( for문1) 바꿔주고(for문2)

버블 정렬
역순으로 정렬
작은 값을 기준으로 앞으로 미는 과정
if문으로 swap되는지를 기준으로 break 걸어서 라운드 끝낼 수 있음

동적 메모리 할당
BYTE * pBufln = (BYTE*)malloc(SIZE);  해당 사이즈만큼 메모리를 힙 영역에 잡기. + 전체 공간은 byte로 할당해 달라
메모리 공간상에 그만큼 만들어지고 pbuf에 넣겠다는 식.
배열에 대한 개수가 예측이 안될 때 사용함
프로그램 실행 중에 잡아준 메모리들은 실행 중에 없애줘야 제대로 종료됨(해제) ---> free(pbufln)